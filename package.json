{
  "name": "mermaid-snapper",
  "version": "0.1.0",
  "private": true,
  "description": "Static browser app for rendering Mermaid diagrams and exporting SVG/PNG assets.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "prepare:pages": "node scripts/prepare-pages.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "mermaid": "^11.15.0"
  },
  "devDependencies": {
    "jest": "^30.3.0"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "docs/javascripts/**/*.js"
    ]
  }
}
